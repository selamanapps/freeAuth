# FreeAuth Server Configuration
# ================================

# Server Configuration
PORT=3000
NODE_ENV=production

# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here
# Optional: Custom banner URLs
# WELCOME_BANNER_URL=https://your-cdn.com/welcome-banner.jpg
# SUCCESS_BANNER_URL=https://your-cdn.com/success-banner.jpg

# CORS Configuration
CORS_ORIGIN=*
# For production, restrict to your domain:
# CORS_ORIGIN=https://yourdomain.com

# Storage Configuration
STORAGE_TYPE=memory
# Options: 'memory' (development) or 'firestore' (production)
# For Firestore, you'll need to set up Firebase service account

# Firebase Configuration (if using Firestore)
# FIREBASE_SERVICE_ACCOUNT={"type": "service_account", ...}
# Get this from Firebase Console > Project Settings > Service Accounts

# Rate Limiting Configuration
# RATE_LIMIT_WINDOW_MS=900000 (15 minutes in ms)
# RATE_LIMIT_MAX_REQUESTS=100

# Logging Configuration
# LOG_LEVEL=info (options: error, warn, info, debug)
# LOG_FILE=/var/log/freeauth/server.log

# Session Configuration
# SESSION_EXPIRY_MS=600000 (10 minutes in ms)

# Webhook Configuration (for external notifications)
# WEBHOOK_TIMEOUT_MS=5000
# WEBHOOK_RETRY_ATTEMPTS=3

# Security Configuration
# HELMET_CSP_ENABLED=true
# RATE_LIMIT_ENABLED=true

# Monitoring
# HEALTH_CHECK_INTERVAL_MS=30000
# METRICS_ENABLED=false

# Deployment
# HOST=0.0.0.0
# CLUSTER_MODE=false
# WORKER_COUNT=1

# Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to version control
# 3. For production, use environment variables from your hosting platform
# 4. For free tier, use STORAGE_TYPE=memory (in-memory) or Firestore free tier
# 5. BOT_TOKEN is required - get from @BotFather on Telegram
